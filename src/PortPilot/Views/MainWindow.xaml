<Window x:Class="PortPilot.Views.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework" Title="PortPilot" Width="1200" Height="760" MinWidth="900" MinHeight="600" WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize" PreviewKeyDown="WindowKeyDown" UseLayoutRounding="True" SnapsToDevicePixels="True">
 <shell:WindowChrome.WindowChrome><shell:WindowChrome CaptionHeight="51" ResizeBorderThickness="6" CornerRadius="14" GlassFrameThickness="0" UseAeroCaptionButtons="False"/></shell:WindowChrome.WindowChrome>
 <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Background="{DynamicResource BackgroundBrush}">
 <Grid><Grid.RowDefinitions><RowDefinition Height="52"/><RowDefinition/><RowDefinition Height="30"/></Grid.RowDefinitions>
  <Grid Background="{DynamicResource SurfaceBrush}"><Grid.ColumnDefinitions><ColumnDefinition Width="190"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
   <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="22,0,0,0"><Border Background="{DynamicResource AccentBrush}" CornerRadius="6" Width="25" Height="25" Margin="0,0,10,0"><TextBlock FontFamily="Segoe Fluent Icons" Text="&#xE968;" Foreground="White" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Center"/></Border><TextBlock Text="PortPilot" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/></StackPanel>
   <Border x:Name="SearchBar" Grid.Column="1" MaxWidth="680" Height="38" Margin="14,0" Background="{DynamicResource SurfaceBrush}" BorderThickness="1" CornerRadius="8" shell:WindowChrome.IsHitTestVisibleInChrome="True">
    <Border.Style><Style TargetType="Border"><Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/><Style.Triggers><DataTrigger Binding="{Binding IsKeyboardFocused, ElementName=SearchBox}" Value="True"><Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/></DataTrigger></Style.Triggers></Style></Border.Style>
    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="101"/><ColumnDefinition Width="1"/><ColumnDefinition/><ColumnDefinition Width="63"/><ColumnDefinition Width="55"/></Grid.ColumnDefinitions>
     <ComboBox x:Name="SearchFieldPicker" ItemsSource="{Binding SearchOptions}" SelectedValuePath="Field" SelectedValue="{Binding SelectedSearchField}" FontSize="12" MinHeight="0" AutomationProperties.Name="搜索字段" ToolTip="选择只在哪个字段中搜索">
      <ComboBox.Template><ControlTemplate TargetType="ComboBox"><Grid>
       <ToggleButton Focusable="False" IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"><ToggleButton.Template><ControlTemplate TargetType="ToggleButton"><Border Background="Transparent" CornerRadius="7,0,0,7"><TextBlock FontFamily="Segoe Fluent Icons" Text="&#xE70D;" FontSize="9" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,9,0"/></Border></ControlTemplate></ToggleButton.Template></ToggleButton>
       <ContentPresenter Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" IsHitTestVisible="False" Margin="11,0,24,0" VerticalAlignment="Center"/>
       <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True" Focusable="False"><Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="4" CornerRadius="8" MinWidth="135"><ScrollViewer MaxHeight="320"><ItemsPresenter/></ScrollViewer></Border></Popup>
      </Grid></ControlTemplate></ComboBox.Template>
     </ComboBox>
     <Border Grid.Column="1" Background="{DynamicResource BorderBrush}" Margin="0,8"/>
     <Grid Grid.Column="2" ClipToBounds="True">
      <TextBox x:Name="SearchBox" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="10,0" BorderThickness="0" Background="Transparent" FontSize="13" VerticalContentAlignment="Center" TextWrapping="NoWrap" ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.HorizontalScrollBarVisibility="Hidden" KeyDown="SearchKeyDown" AutomationProperties.Name="搜索内容" ToolTip="{Binding SearchHint}"/>
      <TextBlock Text="{Binding SearchPlaceholder}" FontSize="12" Foreground="{DynamicResource SecondaryBrush}" Margin="11,0,8,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" IsHitTestVisible="False"><TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding SearchText}" Value=""><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></TextBlock.Style></TextBlock>
     </Grid>
     <CheckBox x:Name="SearchExactToggle" Grid.Column="3" Content="精确" IsChecked="{Binding IsExactSearch}" FontSize="12" Margin="4,0,8,0" VerticalAlignment="Center" ToolTip="完整匹配所选字段，忽略大小写。端口和 PID 始终精确匹配。" AutomationProperties.Name="精确匹配"/>
     <Button x:Name="SearchSubmitButton" Grid.Column="4" Content="搜索" Command="{Binding SubmitSearchCommand}" Padding="7,0" Margin="0,4,5,4" MinHeight="0" ToolTip="{Binding SearchActionHint}" AutomationProperties.Name="执行搜索"/>
    </Grid>
   </Border>
   <StackPanel Grid.Column="2" Orientation="Horizontal" shell:WindowChrome.IsHitTestVisibleInChrome="True"><Border Background="{DynamicResource HoverBrush}" CornerRadius="5" VerticalAlignment="Center" Padding="8,4" Margin="0,0,14,0"><TextBlock Text="{Binding Privilege}" FontSize="10" Foreground="{DynamicResource SecondaryBrush}"/></Border><Button Content="&#xE713;" FontFamily="Segoe Fluent Icons" Command="{Binding NavigateCommand}" CommandParameter="Settings" Background="Transparent" BorderThickness="0" Width="40" ToolTip="设置"/><Button Content="&#xE921;" FontFamily="Segoe Fluent Icons" Click="MinimizeClick" Background="Transparent" BorderThickness="0" Width="42" ToolTip="最小化"/><Button Content="&#xE922;" FontFamily="Segoe Fluent Icons" Click="MaximizeClick" Background="Transparent" BorderThickness="0" Width="42" ToolTip="最大化 / 还原"/><Button Content="&#xE8BB;" FontFamily="Segoe Fluent Icons" Click="CloseClick" Background="Transparent" BorderThickness="0" Width="42" ToolTip="关闭"/></StackPanel>
  </Grid>
  <Grid Grid.Row="1"><Grid.ColumnDefinitions><ColumnDefinition Width="190"/><ColumnDefinition/></Grid.ColumnDefinitions>
   <Border Background="{DynamicResource SidebarBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,1,0"><DockPanel Margin="14,22,14,18">
    <StackPanel DockPanel.Dock="Bottom"><Button Command="{Binding OpenPaletteCommand}" Background="Transparent" HorizontalContentAlignment="Left" BorderThickness="0" Padding="12,9"><StackPanel Orientation="Horizontal"><TextBlock FontFamily="Segoe Fluent Icons" Text="&#xE945;" Margin="0,0,10,0"/><TextBlock Text="Quick commands" FontSize="11"/><TextBlock Text="⌃ K" Foreground="{DynamicResource SecondaryBrush}" FontSize="10" Margin="8,0,0,0"/></StackPanel></Button><Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Margin="8,18,8,0" Padding="0,15,0,0"><StackPanel><StackPanel Orientation="Horizontal"><Ellipse Width="6" Height="6" Fill="{DynamicResource SuccessBrush}" Margin="0,0,7,0"/><TextBlock Text="{Binding ScanHealth}" FontSize="11" Foreground="{DynamicResource SecondaryBrush}"/></StackPanel><TextBlock Text="PORTABLE · v1.0.2" FontSize="9" Foreground="{DynamicResource SecondaryBrush}" Margin="13,7,0,0"/></StackPanel></Border></StackPanel>
    <StackPanel><TextBlock Text="WORKSPACE" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource SecondaryBrush}" Margin="13,0,0,12"/>
     <Button x:Name="DashboardNav" Style="{StaticResource NavButton}" Command="{Binding NavigateCommand}" CommandParameter="Dashboard"><StackPanel Orientation="Horizontal"><TextBlock Text="&#xE80F;" FontFamily="Segoe Fluent Icons" Margin="0,0,13,0"/><TextBlock Text="Dashboard"/></StackPanel></Button>
     <Button x:Name="PortsNav" Style="{StaticResource NavButton}" Command="{Binding NavigateCommand}" CommandParameter="Ports"><StackPanel Orientation="Horizontal"><TextBlock Text="&#xE968;" FontFamily="Segoe Fluent Icons" Margin="0,0,13,0"/><TextBlock Text="Ports"/></StackPanel></Button>
     <Button x:Name="ProcessesNav" Style="{StaticResource NavButton}" Command="{Binding NavigateCommand}" CommandParameter="Processes"><StackPanel Orientation="Horizontal"><TextBlock Text="&#xE7C3;" FontFamily="Segoe Fluent Icons" Margin="0,0,13,0"/><TextBlock Text="Processes"/></StackPanel></Button>
     <Button x:Name="ConnectionsNav" Style="{StaticResource NavButton}" Command="{Binding NavigateCommand}" CommandParameter="Connections"><StackPanel Orientation="Horizontal"><TextBlock Text="&#xE774;" FontFamily="Segoe Fluent Icons" Margin="0,0,13,0"/><TextBlock Text="Connections"/></StackPanel></Button>
     <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Margin="12,18,12,15"/>
     <Button x:Name="FavoritesNav" Style="{StaticResource NavButton}" Command="{Binding NavigateCommand}" CommandParameter="Favorites"><StackPanel Orientation="Horizontal"><TextBlock Text="&#xE734;" FontFamily="Segoe Fluent Icons" Margin="0,0,13,0"/><TextBlock Text="Favorites"/></StackPanel></Button>
     <Button x:Name="HistoryNav" Style="{StaticResource NavButton}" Command="{Binding NavigateCommand}" CommandParameter="History"><StackPanel Orientation="Horizontal"><TextBlock Text="&#xE81C;" FontFamily="Segoe Fluent Icons" Margin="0,0,13,0"/><TextBlock Text="History"/></StackPanel></Button>
     <Button x:Name="SettingsNav" Style="{StaticResource NavButton}" Command="{Binding NavigateCommand}" CommandParameter="Settings"><StackPanel Orientation="Horizontal"><TextBlock Text="&#xE713;" FontFamily="Segoe Fluent Icons" Margin="0,0,13,0"/><TextBlock Text="Settings"/></StackPanel></Button>
    </StackPanel>
   </DockPanel></Border>
   <Grid Grid.Column="1" Margin="28,25,28,22"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/></Grid.RowDefinitions>
    <Grid Margin="0,0,0,24"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{Binding Page}" FontSize="29" FontWeight="SemiBold"/><TextBlock Text="{Binding PageDescription}" Foreground="{DynamicResource SecondaryBrush}" Margin="0,7,0,0" FontSize="12" TextWrapping="Wrap"/></StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top" Margin="12,4,0,0"><Button Content="导出" Command="{Binding ExportCommand}" Margin="0,0,8,0"/><Button Command="{Binding RefreshCommand}" ToolTip="刷新 (F5)"><StackPanel Orientation="Horizontal"><TextBlock FontFamily="Segoe Fluent Icons" Text="&#xE72C;" Margin="0,0,7,0"/><TextBlock Text="刷新"/></StackPanel></Button></StackPanel></Grid>
    <ContentControl x:Name="PageContent" Grid.Row="1" Content="{Binding}"><ContentControl.Style><Style TargetType="ContentControl"><Setter Property="ContentTemplate" Value="{StaticResource DashboardPage}"/><Style.Triggers>
     <DataTrigger Binding="{Binding Page}" Value="Ports"><Setter Property="ContentTemplate" Value="{StaticResource PortsPage}"/></DataTrigger><DataTrigger Binding="{Binding Page}" Value="Processes"><Setter Property="ContentTemplate" Value="{StaticResource ProcessesPage}"/></DataTrigger><DataTrigger Binding="{Binding Page}" Value="Connections"><Setter Property="ContentTemplate" Value="{StaticResource ConnectionsPage}"/></DataTrigger><DataTrigger Binding="{Binding Page}" Value="Favorites"><Setter Property="ContentTemplate" Value="{StaticResource FavoritesPage}"/></DataTrigger><DataTrigger Binding="{Binding Page}" Value="History"><Setter Property="ContentTemplate" Value="{StaticResource HistoryPage}"/></DataTrigger><DataTrigger Binding="{Binding Page}" Value="Settings"><Setter Property="ContentTemplate" Value="{StaticResource SettingsPage}"/></DataTrigger><DataTrigger Binding="{Binding Page}" Value="Search"><Setter Property="ContentTemplate" Value="{StaticResource SearchPage}"/></DataTrigger>
    </Style.Triggers></Style></ContentControl.Style></ContentControl>
   </Grid>
   <Border x:Name="DetailPanel" Grid.Column="1" HorizontalAlignment="Right" Width="370" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1,0,0,0" Visibility="{Binding HasDetails, Converter={StaticResource BoolVisibility}}">
    <Border.Effect><DropShadowEffect BlurRadius="18" ShadowDepth="-4" Opacity="0.10"/></Border.Effect>
    <Grid Margin="20,22,20,18"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
     <Grid Margin="0,0,0,16"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="INSPECTOR" Foreground="{DynamicResource SecondaryBrush}" FontSize="10" Margin="0,0,0,9"/><TextBlock Text="{Binding DetailTitle}" FontSize="21" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding DetailSubtitle}" Foreground="{DynamicResource SecondaryBrush}" FontSize="11" Margin="0,7,0,0" TextWrapping="Wrap"/></StackPanel><Button Grid.Column="1" Content="&#xE8BB;" FontFamily="Segoe Fluent Icons" Command="{Binding CloseDetailsCommand}" VerticalAlignment="Top" Background="Transparent" BorderThickness="0" Padding="8" ToolTip="关闭详情 (Esc)"/></Grid>
     <TabControl x:Name="InspectorTabs" Grid.Row="1"><TabItem Header="Overview"><ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel Margin="0,14,8,0">
      <TextBlock Text="COMMAND LINE" Style="{StaticResource Muted}" Margin="0,0,0,8"/><Border Background="{DynamicResource BackgroundBrush}" CornerRadius="8" Padding="12"><TextBox Text="{Binding DetailCommandLine, Mode=OneWay}" Style="{StaticResource ReadOnlyText}" FontFamily="Cascadia Mono, Consolas"/></Border>
      <Button Content="复制启动参数" Command="{Binding CopyCommand}" CommandParameter="command" HorizontalAlignment="Left" Margin="0,9,0,18" Padding="9,5" FontSize="11"/>
      <ItemsControl ItemsSource="{Binding DetailFields}"><ItemsControl.ItemTemplate><DataTemplate><StackPanel Margin="0,0,0,16"><TextBlock Text="{Binding Label}" Style="{StaticResource Muted}" Margin="0,0,0,5"/><TextBox Text="{Binding Value, Mode=OneWay}" Style="{StaticResource ReadOnlyText}"/></StackPanel></DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
      <StackPanel Visibility="{Binding CanNote, Converter={StaticResource BoolVisibility}}"><TextBlock Text="端口备注" Style="{StaticResource Muted}"/><TextBox Text="{Binding PortNote, UpdateSourceTrigger=PropertyChanged}" Margin="0,8,0,8"/><StackPanel Orientation="Horizontal"><Button Content="保存备注" Command="{Binding SaveNoteCommand}" Margin="0,0,8,0"/><Button Content="收藏端口" Command="{Binding FavoritePortCommand}"/></StackPanel></StackPanel>
     </StackPanel></ScrollViewer></TabItem>
     <TabItem x:Name="NetworkTab" Header="Network"><ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel Margin="0,15,0,0"><TextBlock Text="当前 PID 的全部端点" Style="{StaticResource SectionTitle}"/><ItemsControl x:Name="NetworkItems" ItemsSource="{Binding DetailConnections}"><ItemsControl.ItemTemplate><DataTemplate><Border Background="{DynamicResource BackgroundBrush}" CornerRadius="8" Padding="11" Margin="0,0,8,8"><StackPanel><TextBlock Text="{Binding Local}" FontFamily="Cascadia Mono, Consolas" FontSize="11"/><TextBlock Text="{Binding Remote}" Foreground="{DynamicResource SecondaryBrush}" Margin="0,5,0,0" FontSize="11"/><TextBlock Margin="0,6,0,0" FontSize="10" Foreground="{DynamicResource AccentBrush}"><Run Text="{Binding Protocol, Mode=OneWay}"/> · <Run Text="{Binding State, Mode=OneWay}"/></TextBlock></StackPanel></Border></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></StackPanel></ScrollViewer></TabItem>
     <TabItem Header="More"><ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel Margin="0,15,8,0"><TextBlock Text="Parent / Children" Style="{StaticResource SectionTitle}"/><TextBox Text="{Binding DetailFamily, Mode=OneWay}" Style="{StaticResource ReadOnlyText}" Margin="0,0,0,24"/><TextBlock Text="Environment" Style="{StaticResource SectionTitle}"/><TextBox Text="{Binding DetailEnvironment, Mode=OneWay}" Style="{StaticResource ReadOnlyText}" Margin="0,0,0,24"/><TextBlock Text="Modules" Style="{StaticResource SectionTitle}"/><TextBox Text="{Binding DetailModules, Mode=OneWay}" Style="{StaticResource ReadOnlyText}"/></StackPanel></ScrollViewer></TabItem></TabControl>
     <StackPanel Grid.Row="2" Margin="0,16,0,0"><WrapPanel><Button Content="打开文件位置" Command="{Binding OpenLocationCommand}" Margin="0,0,7,7"/><Button Content="Copy All" Command="{Binding CopyCommand}" Margin="0,0,0,7"/></WrapPanel><WrapPanel><Button Content="正常结束" Command="{Binding KillCommand}" CommandParameter="normal" Style="{StaticResource DangerButton}" Margin="0,0,6,0"/><Button Content="Force Kill" Command="{Binding KillCommand}" CommandParameter="force" Style="{StaticResource DangerButton}" Margin="0,0,6,0"/><Button Content="释放端口" Command="{Binding KillCommand}" CommandParameter="release" Style="{StaticResource DangerButton}"/></WrapPanel></StackPanel>
    </Grid>
   </Border>
  </Grid>
  <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0"><DockPanel Margin="16,0"><TextBlock Text="{Binding LastUpdated}" DockPanel.Dock="Right" Style="{StaticResource Muted}" VerticalAlignment="Center" Margin="14,0,0,0"/><TextBlock Text="{Binding Status}" FontSize="11" Foreground="{DynamicResource SecondaryBrush}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding Status}"/></DockPanel></Border>
  <Grid Grid.RowSpan="3" Background="#66000000" Visibility="{Binding IsPaletteOpen, Converter={StaticResource BoolVisibility}}"><Border Style="{StaticResource Card}" Width="480" MaxHeight="480" VerticalAlignment="Top" Margin="0,95,0,0"><StackPanel><DockPanel><TextBlock Text="Quick commands" FontSize="18" FontWeight="SemiBold"/><Button Content="Esc" Command="{Binding ClosePaletteCommand}" HorizontalAlignment="Right" Padding="7,3" MinHeight="24"/></DockPanel><TextBox x:Name="PaletteSearch" Text="{Binding PaletteQuery, UpdateSourceTrigger=PropertyChanged}" Margin="0,16,0,12" AutomationProperties.Name="搜索命令"/><ListBox x:Name="PaletteList" ItemsSource="{Binding FilteredPaletteCommands}" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextBrush}" SelectedIndex="0" KeyDown="PaletteKeyDown" MouseDoubleClick="PaletteDoubleClick"><ListBox.ItemContainerStyle><Style TargetType="ListBoxItem"><Setter Property="Padding" Value="12,10"/><Setter Property="Foreground" Value="{DynamicResource TextBrush}"/></Style></ListBox.ItemContainerStyle></ListBox></StackPanel></Border></Grid>
 </Grid></Border>
</Window>
